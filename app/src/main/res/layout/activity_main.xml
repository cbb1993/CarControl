<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal">

    <RadioGroup
        android:id="@+id/rg_menu"
        android:layout_width="80dp"
        android:layout_height="match_parent"
        android:background="#fff"
        android:gravity="center"
        android:orientation="vertical">
        <RadioButton
            android:id="@+id/rb_app"
            android:textColor="@drawable/app_select_menu_text_color"
            android:button="@null"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:text="@string/main_menu_app" />
        <RadioButton
            android:id="@+id/rb_music"
            android:textColor="@drawable/app_select_menu_text_color"
            android:button="@null"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:text="@string/main_menu_music" />
        <RadioButton
            android:id="@+id/rb_car"
            android:textColor="@drawable/app_select_menu_text_color"
            android:button="@null"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:text="@string/main_menu_car" />
        <RadioButton
            android:id="@+id/rb_setting"
            android:textColor="@drawable/app_select_menu_text_color"
            android:button="@null"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:text="@string/main_menu_setting" />
    </RadioGroup>

    <FrameLayout
        android:id="@+id/main_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
</LinearLayout>